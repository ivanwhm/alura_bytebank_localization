name: alura_bytebank_i18n
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.0
  sqflite:
  path:
  http:
  http_interceptor:
  uuid:
  flutter_bloc: ^6.1.2
  localstorage: ^3.0.6+9

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  assets:
    - images/bytebank_logo.png
  uses-material-design: true
